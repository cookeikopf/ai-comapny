name: secret-scan
on:
  workflow_dispatch:  # vorübergehend nur manuell startbar

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Skip for now
        run: echo "Secret scan paused; re-enable once repo stabil ist."
